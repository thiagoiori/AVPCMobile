<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.avpc.avpcmobile">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature
        android:name="android.hardware.camera2"
        android:required="false" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="18" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/MaterialTheme">
        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="com.avpc.avpcmobile.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name=".activities.MainActivity"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/MaterialTheme"/>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_application_id" />

        <!-- <activity -->
        <!-- android:name=".activities.NewServiceActivity" -->
        <!-- android:label="@string/title_activity_new_service" -->
        <!-- android:parentActivityName=".activities.MainMenuActivity" -->
        <!-- android:theme="@style/MaterialTheme"> -->
        <!-- <meta-data -->
        <!-- android:name="android.support.PARENT_ACTIVITY" -->
        <!-- android:value="com.avpc.avpcmobile.activities.MainMenuActivity" /> -->
        <!-- </activity> -->

        <activity
            android:name=".map.MapActivity"
            android:parentActivityName=".map.MapActivity"
            android:theme="@style/MaterialTheme"/>
        <activity
            android:name=".services.ServicesActivity"
            android:parentActivityName=".map.MapActivity"
            android:theme="@style/MaterialTheme"/>
        <activity
            android:name=".servicedetails.ServiceDetailsActivity"
            android:parentActivityName=".servicedetails.ServiceDetailsActivity"
            android:theme="@style/MaterialTheme"/>
        <activity
            android:name=".messages.MessagesActivity"
            android:parentActivityName=".messages.MessagesActivity"
            android:theme="@style/MaterialTheme"/>
        <activity
            android:name=".messagedetails.MessageDetailsActivity"
            android:parentActivityName=".messagedetails.MessageDetailsActivity"
            android:theme="@style/MaterialTheme"/>
        <activity
            android:name=".members.MembersActivity"
            android:parentActivityName=".members.MembersActivity"
            android:theme="@style/MaterialTheme"/>
        <activity
            android:name=".memberdetails.MemberDetailsActivity"
            android:parentActivityName=".memberdetails.MemberDetailsActivity"
            android:theme="@style/MaterialTheme"/>
        <activity
            android:name=".vehicles.VehiclesActivity"
            android:parentActivityName=".vehicles.VehiclesActivity"
            android:theme="@style/MaterialTheme"/>
        <activity
            android:name=".vehicledetails.VehicleDetailsActivity"
            android:parentActivityName=".vehicledetails.VehicleDetailsActivity"
            android:theme="@style/MaterialTheme"/>

        <service
            android:name="com.avpc.service.UpdateMemberLocationService"
            android:enabled="true"
            android:exported="true" />

        <activity
            android:name=".addservice.AddServiceActivity"
            android:label="@string/title_activity_add_service"
            android:theme="@style/MaterialTheme" />
        <activity
            android:name=".activities.SplashScreenActivity"
            android:label="@string/title_activity_splash_screen"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>